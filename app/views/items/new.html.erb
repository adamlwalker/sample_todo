<%= form_for([@project, @item]) do |f| %>

  <div class="field">
    <%= f.text_field :action, :class => 'form-control add-todo', :placeholder => 'Add todo item' %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn btn-success'%>
  </div>
<% end %>
